safecode

git add .
git commit -m "tidied the code to store everything in functions and added more words after each one is completed"
git branch -M main
git push -u origin main

git init                   
git remote add origin https://github.com/ishitac04/snakevsblock.git
git add .
git commit -m "added movement for the bee with the pattern changing direction when needed"
git branch -M main
git push -u origin main


TO DO:
- write word
- count no. of words
- add time frame
- make snake faster as progresses
- sound effects?
- highscore?
- fix random glitches